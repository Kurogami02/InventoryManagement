import { Empty } from 'antd';

const NotFoundContent: React.FC = () => {
  return <Empty image={Empty.PRESENTED_IMAGE_SIMPLE} description="Không tìm thấy dữ liệu" />;
};

export default NotFoundContent;
